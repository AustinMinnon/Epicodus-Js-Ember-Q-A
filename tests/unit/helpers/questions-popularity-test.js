import { questionsPopularity } from 'questions-answers/helpers/questions-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | questions popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionsPopularity([42]);
  assert.ok(result);
});
